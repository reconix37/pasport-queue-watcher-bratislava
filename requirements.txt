requests>=2.31
beautifulsoup4>=4.12
# Читання налаштувань із файлу .env (зручно для локального запуску).
python-dotenv>=1.0
# Обхід Cloudflare через імітацію TLS-відбитка браузера (без нього requests → 403).
curl_cffi>=0.7
# playwright потрібен ЛИШЕ для USE_BROWSER=1 (запасний режим браузера).
# За замовчуванням USE_BROWSER=0 і він не використовується, але встановити не завадить.
playwright>=1.44
